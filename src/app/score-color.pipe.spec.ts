import { ScoreColorPipe } from './score-color.pipe';

describe('ScoreColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreColorPipe();
    expect(pipe).toBeTruthy();
  });
});
